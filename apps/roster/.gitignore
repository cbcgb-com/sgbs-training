node_modules/
dist/
.vercel

# Local env files — Convex deploy keys, SMTP secrets, Vercel tokens.
# Never commit these; the repo is public.
.env*
!.env.example

# Local design-review artifacts
.impeccable/
seed/*.jsonl
